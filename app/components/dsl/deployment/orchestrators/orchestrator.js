export default class Orchestrator {

    constructor() {
        
    }

    async deploy(services) {

    }

    async undeploy(services) {

    }

    /**
     * Returns a mapping between serviceHost and proxyHost.
     * @returns {[serviceHost: proxyHost]}
     */
    getMapping() {

    }

}
